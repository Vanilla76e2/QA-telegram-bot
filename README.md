# Telegram Question Bot

**Telegram Question Bot** — это бот для приема вопросов от пользователей и их удобного управления менеджерами через Telegram. Он поддерживает текстовые сообщения, фото и видео, а также inline-кнопки для смены статусов вопросов и пагинацию списка вопросов.

---

## Основные возможности

- Пользователи могут задавать вопросы с текстом и медиа (фото/видео).  
- Менеджеры видят список вопросов с фильтрацией по статусу.  
- Поддержка пагинации для удобного просмотра большого числа вопросов.  
- Inline-кнопки для мгновенного изменения статуса вопроса.  
- Уведомления пользователей о смене статуса их вопроса.  
- Сортировка вопросов по дате создания (от старого к новому).  
- Медиа и текст объединены в одно сообщение при отправке менеджерам.

---

## Статусы вопросов

- `новый 🆕`
- `в работе ⚙️`
- `выполнено ✅`
- `отклонено ❌`

---

## Установка и запуск

1.  Клонируйте репозиторий:

   ```bash
   git clone https://github.com/username/telegram-question-bot.git
   cd telegram-question-bot
   ```

2. Создайте виртуальное окружение и установите зависимости:

```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
venv\Scripts\activate     # Windows
pip install -r requirements.txt
```

3. Рекомендуется запускать бота через встроенный CLI, который позволяет удобно управлять настройками и самим ботом.  

После запуска CLI вы увидите меню:

```bash
=== Telegram Bot CLI ===
1. Показать настройки
2. Изменить настройку
3. Запустить бота
4. Остановить бота
5. Перезапустить бота
6. Показать логи
7. Выход
> 
```

Через это меню вы можете:
- Просматривать и менять настройки бота, включая токен и рабочий чат;
- Запускать, останавливать и перезапускать бота;
- Просматривать логи работы.

CLI автоматически создаёт `config.json` при первой настройке, поэтому отдельная ручная правка файла не требуется.  

Просто выберите нужный пункт в меню и следуйте подсказкам.

---

## Использование

### Пользователь

- Отправляет сообщение с текстом и/или фото/видео.
    
- Бот принимает вопрос и подтверждает его создание.

### Менеджер

- Получает уведомление о новых вопросах с кнопками для смены статуса.
    
- Может просматривать список вопросов с пагинацией (`📋 Список вопросов`).
    
- Меняет статус вопроса нажатием на inline-кнопки.
    
- Получает уведомления о статусе вопросов пользователей.

---

## Структура проекта
```bash
├─ handlers/       # Обработчики сообщений и команд
├─ keyboards/      # Генерация inline-клавиатур и кнопок
├─ logs/           # Папка для логов работы бота
├─ models.py       # SQLAlchemy модели
├─ database.py     # Настройка базы данных
├─ helpers.py      # Вспомогательные функции (отправка медиа, уведомления)
├─ loader.py       # Точка входа, инициализация бота
├─ config.py       # Чтение конфигурации из config.json
├─ config.json     # Автоматически создаётся CLI при первой настройке
├─ questions.db    # Автоматически создаётся база данных
├─ requirements.txt
├─ README.md
```

## Лицензия

MIT License.